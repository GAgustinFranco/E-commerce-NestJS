import { DateAdderInterceptorInterceptor } from './date-adder-interceptor.interceptor';

describe('DateAdderInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new DateAdderInterceptorInterceptor()).toBeDefined();
  });
});
